import { Text } from 'react-native';
import * as Styled from './ShoppingCart.styles';

function ShoppingCart() {

    
    return (
        <Text style={{fontSize: 30, color: "#000000", textAlign: "center"}}>
            ShopingCart screen
        </Text>
    );
};

export default ShoppingCart;